// jest.config.js
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'jsdom', // Use 'jsdom' for browser-like environment
  testTimeout: 30000, // Set a timeout of 20 seconds (20000 ms) for the entire test suite
};
